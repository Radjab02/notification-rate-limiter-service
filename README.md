# notification-rate-limiter-service
The service is a Spring Boot app that enforces rate limits for notification endpoints. It implements a three-tier rate limiting strategy: Global system-level, Client monthly limit, Client per-window limit
